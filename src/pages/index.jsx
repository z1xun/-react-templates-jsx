import React from 'react';
import Layout from '../components/layout/Layout';

const Home = () => {
    return (
        <Layout title="Home" pagename="home">
            home
        </Layout>
    );
};

export default Home;
